Formating

 print("{} Test1: {}  Test2: {}  Test3: {}  Gemiddelde: {}  ".format(naam, test1, test2, test3, gemiddelde))

// {} zorgt er voor dat elke var tussen de {} komen in vollegorde



for aantal in range(1, 101): // geeft u de getallen van 1 tot 100



teller = 1
var = input("geef een eerst getal in")
while var != "":

    teller += 1
    gemiddelde2 =+ int(var)
    var = input("geef een eerst getal in")

    if teller == 3:
        som = gemiddelde2 / teller
        teller = 0
        print(som)

// voorbeeld van een while lus

tekst += chr(random.randint(ord("A"), ord("Z"))) // voorbeeld van een random letter van a tot z



 while not (productcode[6:] == "bo" or productcode[6:] == "on"):
    productcode = input("De productcode eindingt niet met 'bo' of 'on' ")
// zorgt er voor dat na teken 6 dat ze gaan controleren hier begin

Oef 7.2 kan handig zijn in extra

7.9 en 7.8 ook geen extra