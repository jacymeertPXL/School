/*const square = x => {
    console.log(x);
    return x * x;
};

console.log(square());
console.log(square(5, 3, 'letter'));
console.log(square("i"));
console.log(square(3));*/

/*
let minus = (a, b) => {
    console.log(a)
    if (b == undefined) {
        return -a;
    } else {
        return a - b;
    }
}

console.log(minus());
console.log(minus(5));
console.log(minus(5, 6))
console.log(minus(5, 6, "letter"));*/

/*
const hummus = function(factor) { // twee functions in elkaar deze werken alleen omdat de eerste functiede 2de oproept en uitvoert
    const ingredient = function(amount, unit, name) {
        let ingredientAmount = amount * factor;
        if (ingredientAmount > 1) {
            unit += "s";
        }
        console.log(`${ingredientAmount} ${unit} ${name}`);
    };
    ingredient(1, "can", "chickpeas");
    ingredient(0.25, "cup", "tahini");
    ingredient(0.25, "cup", "lemon juice");
};

hummus(3);*/
